export interface DashboardWidget {

    widget_code: string;


    category: string;

    // position in grid
    pos_x: number;
    pos_y: number;

    // current size
    width: number;
    height: number;

}